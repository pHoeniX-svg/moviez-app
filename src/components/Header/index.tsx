import { GlobalContainer } from '~src/globals';
import { Container } from './style';

const Header = () => {
  return (
    <Container>
      <GlobalContainer></GlobalContainer>
    </Container>
  );
};

export { Header };
